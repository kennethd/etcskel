# this is an unfortunate hack, but its not worth finding a better way.
# list files here which were formerly provided by this package.
# the delete code should also respect the no-overwrite file, for users who
# want to keep the old version, or happen to re-use the path for something else
# 
# like no-overwrite
bin/etcskel-dropbox-backups-sync
# moved to etc/default
etc/defaults/crontab
etc/defaults
# moved to etc/logrotate.d
etc/logrotate
.bash/aliases.d/README
.bash/aliases.d
.bash/functions.d/README
.bash/functions.d
.bash
